"""Skill functions — the actions the executor can perform.

Each module exposes plain functions that return a uniform dict:
    {"ok": bool, "speak": Optional[str], "message": Optional[str], ...extra}
"""
